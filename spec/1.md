### 需求

参照event order的copy和paste的功能实现event order items的copy和paste的功能

### 要求：

1. copy的样式样式参照event order的copy
2. 选中event order items点击copy按钮后，event和event order items前面的勾选框不能勾选，只有event order
3. 选中event order items点击copy按钮后，需要展示的按钮和event order点击copy按钮后一样

### 补充

1. 如果项目中缺少部分文件代码，可以要求补充
2. 先给出方案和需要改动的文件
3. 缺失的文件已补充，请根据新的文件给出方案，代码尽量可以复用，例如组件<PasteActionBar />是否可以复用
4. 组件<PasteActionBar />是复用好还是不复用好

### 问题

1.代码中event order和event order item copy场景下是否可以使用applyTransaction来代替redraw的实现，
是否可以这样勾选的event order item在点击copy按钮后，添加一个变量是否处于copy状态控制，这个变量通过applyTransaction来更新，当处于copy状态时，添加样式，不处于copy状态时，样式消失，以这个思路来替换redraw
现在的redraw方案是否会消耗更多性能，比如在event order中，如果有选中event order item，在copy状态下展开和折叠时只是样式更改，但是会重复redraw，可否参考src/pages/bookings/events/actions/DefaultActionbar.tsx文件中saveChangeEvents方法的实现思路

按照这个思路，src/hooks/useOrderGrid.ts文件中的onRowGroupOpened: (\_event: RowGroupOpenedEvent<EventOrderProps>) => {
const orderRowKey = getOrderRowKey(\_event.data!);
const { selectedItems, isCopyItem } = useEventStore.getState();
const hasSelectedItems = Array.from(selectedItems.values()).some(
(item) => item.orderRowKey === orderRowKey,
);
if (hasSelectedItems && isCopyItem) {
\_event.api.redrawRows({ rowNodes: [_event.node] });
}
resetGridRowHeight();
},方法中redrawRows到底干了什么

文件中这块代码的主要作用只是在更改样式，是否可以根据isCopyItem通过在rowClassRules中配置样式去加 border-dashed或移除 border-dashed

这个applyTransaction是否可以放到event上，如果放到event上，那么会触发event order和event order item的样式更新吗？

2. 现在还有个bug，先勾选event order item，再点击copy，收起event，再点击cancel copy时，在event上加的border-dashed没有被移除,
   现在还是没有被移除，应该是点击cancel copy时没有redraw
   改的不对，应该是useRedrawSelectedItems中没有redraw event
