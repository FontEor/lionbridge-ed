### 需求

参照event order的copy和paste的功能实现event order items的copy和paste的功能

### 要求：

1. copy的样式样式参照event order的copy
2. 选中event order items点击copy按钮后，event和event order items前面的勾选框不能勾选，只有event order
3. 选中event order items点击copy按钮后，需要展示的按钮和event order点击copy按钮后一样

### 补充

1. 如果项目中缺少部分文件代码，可以要求补充
2. 先给出方案和需要改动的文件
3. 缺失的文件已补充，请根据新的文件给出方案，代码尽量可以复用，例如组件<PasteActionBar />是否可以复用
4. 组件<PasteActionBar />是复用好还是不复用好

### redraw的原因

核心原因是 copy 状态存在 Zustand store 里，不在 row data 里，而 ag-grid 只在 row data 变化时才会重新计算 rowClassRules

### 问题

1. 代码中勾选的event order item在点击copy按钮后redraw的场景下是否可以使用applyTransaction来代替redraw的实现，当处于copy状态时，添加样式，不处于copy状态时，样式消失，以类似于errors 这个思路来实现，
   文件中redraw代码的主要作用只是在更改样式，比如可以根据isCopyItem通过在rowClassRules中配置样式去加 border-dashed或移除 border-dashed，这个applyTransaction是否可以放到event上，如果放到event上，那么会触发event order和event order item的样式更新吗？

2. 代码中勾选的event order item在点击copy按钮添加样式，为什么要redraw呢
   src/hooks/useGridSelect.ts文件中

3. src/pages/bookings/events/actions/ItemSearchDrawer/index.tsx文件中有个bug，是在添加item时，需要拿到这个item,只需要这个item走if (item.eventOrderItemSid) return item;
   return {
   ...item,
   serveDtTm: order.startDateTime,
   quantityOrdered: order.attendanceEstimate,
   quantityGuaranteed: order.attendanceGuarantee,
   };
   这个逻辑，而不需要所有的item都走这个逻辑
