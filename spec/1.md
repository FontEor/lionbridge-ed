### 需求

参照event order的copy和paste的功能实现event order items的copy和paste的功能

### 要求：

1. copy的样式样式参照event order的copy
2. 选中event order items点击copy按钮后，event和event order items前面的勾选框不能勾选，只有event order
3. 选中event order items点击copy按钮后，需要展示的按钮和event order点击copy按钮后一样

### 补充

1. 如果项目中缺少部分文件代码，可以要求补充
2. 先给出方案和需要改动的文件
3. 缺失的文件已补充，请根据新的文件给出方案，代码尽量可以复用，例如组件<PasteActionBar />是否可以复用
4. 组件<PasteActionBar />是复用好还是不复用好

### 问题

1.src/index.css文件中现在有个问题，当event下有多个event order时，勾选第一个event order下的event order item，点击copy后，会导致最后一个event order下的border-bottom添加上了下面的样式
.ag-center-cols-viewport:has(.ag-row-last.border-dashed){
border-bottom: 2px dashed var(--color-main-sky-500) !important;
}
这样也不行，换个思路是否可以在勾选event order item点击copy后，如果是当前event order的最后一个，在文件中src/hooks/useItemGrid.ts将其高度设置为32px
这样加的样式和原来的border-bottom不是重合的
